description: RM automation
contact: "#support-frontend-frameworks"
image: drydock.workiva.net/workiva/skynet-images:bash_curl_alpinelatest-latest

size: small
timeout: short

run:
  on-tag: true
  on-pull-request: true

scripts:
  - echo "No tests currently set"
